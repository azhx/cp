#include <bits/stdc++.h>
using namespace std;
#define toDigit(x) (x-'0')

int main () {
  int N;
  cin >> N;
  vector <> results;s
  for (int i = 0; i < N; i++) {
  }
  for (unsigned int i = 0; i < results.size(); i++) {
    cout << results[i] << endl;
  }
  return 0;
}
